<?xml version="1.0" encoding="utf-8"?>
<!--
  One self-contained Parrot.exe: nothing to install, runs from anywhere.
  dotnet publish src/Parrot.App -p:PublishProfile=win-x64
-->
<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <!-- SQLite and WPF ship native DLLs; bundle them instead of leaving them next to the exe. -->
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
    <DebugType>none</DebugType>
    <PublishDir>..\..\artifacts\publish\</PublishDir>
  </PropertyGroup>
</Project>
